# 多面并行阵（MultiFace ZhenBus）架构设计

## 1. 概述

### 1.1 目标

实现一个方块（称为 **ZhenBus**），在其 **6 个面**上各自独立安装、运行不同类型的 **阵（ZhenType）**，各面互不干扰、并行运作。效果等价于「一个方块中同时运行 6 个独立的方块实体」。

### 1.2 核心思想

**以面（Direction）为分区单位，每个面是一个独立的处理器单元。**

```
                    ┌─────────────┐
                    │   UP 面      │
                    │  火阵(Type1) │
                    ├──────┬──────┤
          ← 西面     │      │      │  东面 →
         水阵(Type2) │      │      │  土阵(Type3)
                    ├──────┴──────┤
                    │   DOWN 面    │
                    │  风阵(Type4) │
                    └─────────────┘
```

### 1.3 与 AE2 Multipart 的对应关系

| AE2 | MagicIO | 说明 |
|-----|---------|------|
| `CableBusBlock` | `ZhenBusBlock` | 方块本身，无功能，作为容器 |
| `CableBusBlockEntity` | `ZhenBusBlockEntity` | 方块实体，管理所有面的状态 |
| `CableBusContainer` | `ZhenBusContainer` | 6 个面的统一管理逻辑 |
| `CableBusStorage` | `ZhenBusStorage` | 纯数据结构，存储 6 个面的数据 |
| `IPart` | `SideProcessor` | 每个面的处理单元接口 |
| `AEBasePart` | `AbstractSideProcessor` | 处理单元基类 |
| 6 个方向 Part 数组 | `Map<Direction, SideProcessor>` | 6 个面的存储 |
| 中心 CablePart | 无（ZhenBus 无中心线缆） | — |

---

## 2. 架构总览

```
┌──────────────────────────────────────────────────────────────────┐
│                       ZhenBusBlock（方块）                       │
│  1. 派生自 BaseEntityBlock                                      │
│  2. 渲染分发：通过 Direction 判断用户点击了哪个面               │
│  3. 碰撞箱：合并所有已安装面的碰撞箱                            │
│  4. 获取掉落物：遍历所有面                                      │
└──────────────────────────┬───────────────────────────────────────┘
                           │ 创建 / tick / 交互
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                   ZhenBusBlockEntity（方块实体）                  │
│  1. 实现 ZhenBusHost 接口                                        │
│  2. serverTick → 遍历所有已安装面并 tick                         │
│  3. NBT 序列化 → 保存/读取每个面的独立状态                       │
│  4. Capability 分发 → 按方向+IO类型代理到对应的 SideProcessor   │
│  5. 网络同步 → 只同步状态变化的面                                │
└──────────────────────────┬───────────────────────────────────────┘
                           │ 持有
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                    ZhenBusContainer（容器逻辑）                   │
│  1. 持有 Map<Direction, SideProcessor>                           │
│  2. addProcessor / removeProcessor / getProcessor               │
│  3. tickAll → 遍历所有 processor 执行 tick                       │
│  4. 碰撞箱合并 → combineAllShapes()                              │
│  5. 面冲突检测 → 不能在同一面安装两个处理器                      │
└──────────────────────────┬───────────────────────────────────────┘
                           │ 存储
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                     ZhenBusStorage（数据存储）                    │
│  Map<Direction, SideProcessorData>                               │
│  每个方向存储：ZhenType + IOProcessor 数据 + Tick 状态           │
└──────────────────────────┬───────────────────────────────────────┘
                           │ 每个面
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│              AbstractSideProcessor（面处理器基类）                │
│  1. 持有 direction、zhenType、IOProcessor                        │
│  2. 持有独立的 items、tanks、energy 组件                         │
│  3. 独立的 tick() 逻辑（复用现有 ZhenType.execute）              │
│  4. 独立的 NBT 序列化                                            │
│  5. 独立的面访问控制                                             │
└──────────────────────────────────────────────────────────────────┘
```

---

## 3. 核心接口设计

### 3.1 ZhenBusHost — 宿主接口

```java
public interface ZhenBusHost {
    @Nullable SideProcessor getProcessor(Direction direction);
    boolean canAddProcessor(Direction direction, ZhenType type);
    <T extends SideProcessor> T addProcessor(
        ZhenType type, Direction direction, Player player
    );
    void removeProcessor(Direction direction);
    void markForUpdate();
    void markForSave();
    boolean isEmpty();
    BlockEntity getBlockEntity();
}
```

### 3.2 SideProcessor — 面处理器接口

```java
public interface SideProcessor {
    /** 所在方向 */
    Direction getSide();

    /** 绑定的阵类型 */
    ZhenType getZhenType();

    /** IO 处理器（内部含 item/fluid/energy 组件） */
    IOProcessor getIOProcessor();

    /** 每 tick 执行 */
    void tick();

    /** 碰撞箱（用于交互和渲染） */
    VoxelShape getShape();

    /** NBT 读写 */
    void writeToNBT(CompoundTag tag);
    void readFromNBT(CompoundTag tag);

    /** 网络同步 */
    void writeToStream(FriendlyByteBuf buf);
    boolean readFromStream(FriendlyByteBuf buf);

    /** 面访问控制：外部管道从 direction 访问时的 IO 权限 */
    @Nullable Map<IOType, Set<Integer>> getFaceAccess(Direction direction);

    /** 交互 */
    boolean onActivate(Player player, InteractionHand hand, Vec3 hitPos);
    boolean onShiftActivate(Player player, InteractionHand hand, Vec3 hitPos);

    /** 掉落物 */
    void addDrops(List<ItemStack> drops);

    /** 生命周期 */
    void onAdd();
    void onRemove();
}
```

### 3.3 ZhenBusContainer — 容器逻辑

```java
public class ZhenBusContainer {
    private final Map<Direction, SideProcessor> processors = new EnumMap<>(Direction.class);

    // CRUD
    public @Nullable SideProcessor get(Direction dir) { ... }
    public boolean canAdd(Direction dir, ZhenType type) { ... }
    public <T extends SideProcessor> T add(
        ZhenType type, Direction dir, Player player
    ) { ... }
    public void remove(Direction dir) { ... }
    public boolean isEmpty() { ... }

    // Tick
    public void tickAll() {
        for (SideProcessor processor : processors.values()) {
            processor.tick();
        }
    }

    // 碰撞箱
    public VoxelShape getCombinedShape() { ... }

    // 点击命中
    public @Nullable SideProcessor selectProcessor(Vec3 hitPos) { ... }

    // 序列化
    public void writeToNBT(CompoundTag tag) { ... }
    public void readFromNBT(CompoundTag tag) { ... }
}
```

---

## 4. 数据存储方案

### 4.1 NBT 存储结构

每个方向的处理器存储在自己的 Compound 中：

```nbt
ZhenBusBlockEntity
├── processors: Compound
│   ├── "up": Compound
│   │   ├── "type": "magicio:fire_zhen"      // ZhenType ID
│   │   ├── "items": List<Compound>           // 物品栏
│   │   ├── "tanks": List<Compound>           // 流体槽
│   │   ├── "energy": Int                     // 能量
│   │   ├── "processTime": Int
│   │   ├── "currentRecipe": Compound
│   │   └── "ioFaceAccess": Compound
│   ├── "down": Compound
│   │   └── ...
│   ├── "north": Compound
│   ├── "south": Compound
│   ├── "east": Compound
│   └── "west": Compound
└── ...
```

### 4.2 物品/流体/能量 分离

每个方向独立持有自己的存储：

```java
public class SideProcessorData {
    private final Direction direction;
    private final ZhenType zhenType;
    private final IOProcessor ioProcessor;     // 内含独立的物品/流体/能量组件
    private final SlotPartition partition;     // 该面的槽位分区
    private int processTime;
    private @Nullable ZhenRecipe currentRecipe;
    private boolean inputsChanged;
}
```

**存储容量计算**：总容量 = 6 × 每个面的容量。
例如每个面有 2 个物品槽，总物品槽数 = 12。分别在独立的 `NonNullList<ItemStack>` 中。

---

## 5. 面处理器实现（AbstractSideProcessor）

### 5.1 基类设计

```java
public abstract class AbstractSideProcessor implements SideProcessor {
    protected final Direction side;
    protected final ZhenType zhenType;
    protected final IOProcessor ioProcessor;
    protected final NonNullList<ItemStack> items;
    protected final NonNullList<FluidStack> tanks;
    protected final SlotPartition partition;
    protected int processTime = 0;
    protected boolean inputsChanged = false;
    protected @Nullable ZhenRecipe currentRecipe;
    protected Runnable onChanged;

    public AbstractSideProcessor(Direction side, ZhenType zhenType) {
        this.side = side;
        this.zhenType = zhenType;
        this.partition = zhenType.getPartition();
        int itemSlots = partition.getTotalSlots(ModIOTypes.ITEM.get());
        int fluidSlots = partition.getTotalSlots(ModIOTypes.FLUID.get());
        this.items = NonNullList.withSize(itemSlots, ItemStack.EMPTY);
        this.tanks = NonNullList.withSize(fluidSlots, FluidStack.EMPTY);
        this.ioProcessor = new IOProcessor();
        initIOComponents();
    }

    private void initIOComponents() {
        ioProcessor.register(new ItemIOComponent(items, partition));
        ioProcessor.register(new FluidIOComponent(tanks, partition, zhenType.getTankCapacity()));
        if (zhenType.getEnergyCapacity() != null) {
            ioProcessor.register(new EnergyIOComponent(
                zhenType.getEnergyCapacity(), 1
            ));
        }
    }

    @Override
    public void tick() {
        // 直接复用现有的 ZhenType.execute() 逻辑
        // 但需要替换 AbstractZhenBlockEntity 为 SideProcessor 自身的上下文
        if (zhenType.getTickFactory() != null) {
            zhenType.getTickFactory().apply(new SideProcessorMethod(level, pos, state, this)).run();
        }
    }
}
```

### 5.2 复用现有系统

| 现有组件 | 复用方式 |
|---------|---------|
| `IOProcessor` | 每个 SideProcessor 持有自己的实例 |
| `IOComponent` | 原样复用 |
| `SlotPartition` / `SlotZone` | 原样复用，每个面独立配置 |
| `ZhenType` | 原样复用，每个面绑定一个 |
| `ZhenRecipe` / `ZhenRecipeManager` | 原样复用 |
| `SmallSiftMethod` / 其他 Method | 需要创建 `SideProcessorMethod` 替代，将 `blockEntity` 替换为 `sideProcessor` |

### 5.3 与现有 AbstractZhenBlockEntity 的关系

**现有的 AbstractZhenBlockEntity 演化方向：**

```
AbstractZhenBlockEntity (当前: 单一方块处理器)
         │
         ├── 保留为轻量级单一处理器（用于简单方块）
         │
         └── 其核心 tick 逻辑 → 提取为
              AbstractSideProcessor 的内部 tick 方法
              （供 ZhenBusBlockEntity 中的每个面调用）
```

**关键改造**：将 `AbstractZhenBlockEntity.serverTick()` 中的处理逻辑提取为一个**无状态的静态方法**，接受 `SideProcessor` 作为参数：

```java
// 原有 serverTick 中的核心逻辑 → 提取为工具方法
public class RecipeProcessor {
    public static void processTick(SideProcessor processor, Level level) {
        // 复用现有逻辑：配方匹配、进度推进、产出
        // ... 从 AbstractZhenBlockEntity.serverTick 中提取的代码 ...
    }
}
```

---

## 6. ZhenBusBlock 方块设计

### 6.1 方块类

```java
public class ZhenBusBlock extends BaseEntityBlock {
    // 与普通方块不同的关键行为：

    @Override
    public InteractionResult useWithoutItem(BlockState state, Level level,
            BlockPos pos, Player player, BlockHitResult hit) {
        // 1. 获取点击的面 Direction
        Direction clickedFace = hit.getDirection();

        // 2. 获取方块实体
        if (level.getBlockEntity(pos) instanceof ZhenBusBlockEntity host) {

            // 3. 判断点击的面是否有处理器
            SideProcessor processor = host.getProcessor(clickedFace);

            if (processor != null) {
                // 4. 如果有，转发交互到该处理器
                return processor.onActivate(player, player.getUsedItemHand(), hit.getLocation());
            }

            // 5. 如果没有，尝试安装新的处理器（使用手持物品中的 ZhenType）
            // 由 Item 处理
        }
        return InteractionResult.PASS;
    }

    @Override
    public VoxelShape getShape(BlockState state, BlockGetter level,
            BlockPos pos, CollisionContext ctx) {
        if (level.getBlockEntity(pos) instanceof ZhenBusBlockEntity host) {
            return host.getCombinedShape();
        }
        return Shapes.block();
    }

    @Override
    public void playerDestroy(Level level, Player player, BlockPos pos,
            BlockState state, @Nullable BlockEntity blockEntity, ItemStack stack) {
        if (blockEntity instanceof ZhenBusBlockEntity host) {
            // 掉落所有面的物品和处理器
            host.addDrops(drops);
        }
        super.playerDestroy(...);
    }
}
```

### 6.2 碰撞箱设计

每个面处理器的碰撞箱由其 `ZhenType` 定义，例如贴附在面上的扁平形状：

```
   ┌──────┐
   │ 终端  │  ← 凸出 2 像素
   ├──┬───┤
   │线│   │  ← 线缆部分 4×4×4
   │缆│   │
   └──┴───┘
```

或更简单的**扁平贴附**：

```
   ╔══════╗
   ║ 面板  ║  ← 凸出 1 像素，扁平贴附
   ╚══════╝
     ██     ← 线缆交叉点
```

碰撞箱通过合并所有面的碰撞箱生成，Minecraft 原生支持 `VoxelShape` 的 OR 操作：

```java
public VoxelShape getCombinedShape() {
    VoxelShape shape = Shapes.empty();
    for (SideProcessor processor : processors.values()) {
        shape = Shapes.or(shape, processor.getShape());
    }
    return shape.optimize();
}
```

---

## 7. Capability 分发

### 7.1 外部管道访问

当外部管道（如其他模组的物品/流体管道）从某个方向连接时：

```java
// ZhenBusBlockEntity.getCapability()
@Override
public <T> LazyOptional<T> getCapability(Capability<T> cap, @Nullable Direction side) {
    if (side == null) return LazyOptional.empty();

    SideProcessor processor = container.get(side);
    if (processor == null) return LazyOptional.empty();

    // 获取该面定义的对外访问权限
    Map<IOType, Set<Integer>> faceAccess = processor.getFaceAccess(side);

    // 根据 Capability 类型代理到对应的 IOComponent
    if (cap == Capabilities.ITEM) {
        return LazyOptional.of(() -> new SideItemHandler(processor, faceAccess));
    }
    if (cap == Capabilities.FLUID) {
        return LazyOptional.of(() -> new SideFluidHandler(processor, faceAccess));
    }
    // ...
}
```

### 7.2 各面之间的交互（可选）

如果面与面之间需要通信（如能量传输）：

```java
// 在 ZhenBusBlockEntity 中提供跨面访问
public class ZhenBusBlockEntity ... {
    public @Nullable IOProcessor getProcessorIO(Direction side) {
        SideProcessor processor = container.get(side);
        return processor != null ? processor.getIOProcessor() : null;
    }

    // 或者提供相邻面查询
    public @Nullable SideProcessor getNeighbor(Direction fromSide, Direction direction) {
        // 例如：从 UP 面的 NORTH 方向获取相邻的处理器
        // 这将由具体的 ZhenType 逻辑决定是否需要
    }
}
```

---

## 8. 物品/安装/交互

### 8.1 安装方式

**方式一：手持物品右击特定面安装（推荐）**

1. 玩家手持"阵物品"（携带 ZhenType 数据的 ItemStack）
2. 右击 ZhenBus 方块的**某个面**
3. `ZhenBusBlock.onActivate()` 被调用
4. 获取点击的 `Direction`
5. 检查该面是否为空
6. 从物品中读取 `ZhenType`（通过 `DataComponent`）
7. 调用 `host.addProcessor(zhenType, clickedFace, player)`
8. 创建 `SideProcessor` 实例
9. 消耗物品

**方式二：潜行右击拆除**

1. 玩家潜行右击某个面
2. `onShiftActivate()` 被调用
3. 查询点击的面
4. 移除该面的处理器
5. 掉落对应的物品

### 8.2 物品形式

每个 ZhenType 需要一个对应的物品，用于手持安装：

```java
public class ZhenBusPartItem extends Item {
    public ZhenBusPartItem(Properties props) { ... }

    @Override
    public InteractionResult useOn(UseOnContext context) {
        // 1. 获取点击的方块
        // 2. 如果是 ZhenBusBlock，获取点击面
        // 3. 读取 DataComponent 中的 ZhenType
        // 4. 调用 ZhenBusHost.addProcessor()
        // 5. 返回 SUCCESS
    }
}
```

---

## 9. 渲染

### 9.1 合并模型渲染

参考 AE2 的 `CableBusBakedModel`，采用**合并模型**策略：

```java
public class ZhenBusBakedModel implements IBakedModel {
    @Override
    public ModelData getModelData(...) {
        // 收集所有面的模型数据
        Map<Direction, SideProcessor> processors = ...
        return ModelData.builder()
            .with(ZhenBusModelData.PROCESSORS, processors)
            .build();
    }

    @Override
    protected void emitBlockQuads(...) {
        // 遍历所有已安装的面，分别获取其模型并渲染
        for (var entry : processors.entrySet()) {
            IBakedModel partModel = entry.getValue().getModel();
            partModel.emitBlockQuads(...);
        }
    }
}
```

### 9.2 简化方案

如不需要自定义渲染，可以直接使用方块实体特殊的 `BlockEntityWithoutLevelRenderer`，或简单的 JSON 模型：

```
zhebus_block.json → 只是一个六面框架
                   + 每个面根据处理器类型叠加 overlay 模型
```

---

## 10. 与现有系统的兼容性

### 10.1 现有块实体的演变

| 当前 | 未来 | 影响 |
|------|------|------|
| `ZhenBlock` | 保留为**单一阵方块** | 兼容旧方块 |
| `ZhenBlockEntity` | 保留为单一处理器 | 轻量级使用场景 |
| `AbstractZhenBlockEntity` | 核心 tick 逻辑 → 提取到 `RecipeProcessor` 工具类 | 重构，不破坏API |
| `ZhenType.execute()` | 复用，传入 `SideProcessor` 替代 `AbstractZhenBlockEntity` | 需调整参数类型 |

### 10.2 新增 vs 修改

| 文件 | 操作 | 说明 |
|------|------|------|
| `ZhenBusBlock.java` | **新建** | 多面阵方块 |
| `ZhenBusBlockEntity.java` | **新建** | 多面阵方块实体 |
| `ZhenBusContainer.java` | **新建** | 容器管理逻辑 |
| `ZhenBusStorage.java` | **新建** | 数据存储 |
| `ZhenBusHost.java` | **新建** | 宿主接口 |
| `SideProcessor.java` | **新建** | 面处理器接口 |
| `AbstractSideProcessor.java` | **新建** | 面处理器基类 |
| `SideProcessorMethod.java` | **新建** | 替代 SmallSiftMethod，接受 SideProcessor |
| `ZhenBusPartItem.java` | **新建** | 阵部件物品 |
| `RecipeProcessor.java` | **新建** | 从 AbstractZhenBlockEntity 提取的 tick 工具 |
| `AbstractZhenBlockEntity.java` | **修改** | 解耦 tick 逻辑，复用 `RecipeProcessor` |
| `SmallSiftMethod.java` | **修改** | 保持向后兼容，或保留与 SideProcessorMethod 共存 |

---

## 11. 变体方案讨论

### 方案 A：统一容器方案（推荐 ✅）

**核心思想**：1 个方块实体，内部管理 6 个独立的 `SideProcessor`

**优点：**
- 与 AE2 类似，有成功先例
- 共享方块实体的网络同步、标记更新等基础设施
- 内存开销小（1 个 BE vs 6 个 BE）
- NBT 读写统一管理
- 面之间的交互（能量传输等）易于实现

**缺点：**
- 单个 BE 的 tick 逻辑变复杂
- NBT 体积增大
- 单个 BE 的渲染更复杂

---

### 方案 B：9 个独立 BE 方案

**核心思想**：1 个中心方块（ZhenCore）+ 6 个独立的小 BE（SideBlock），每个 SideBlock 就是一个现有 ZhenBlock

**优点：**
- 完全复用现有架构，无需重构
- 每个 BE 简单独立

**缺点：**
- 占用 7 个方块空间，影响美观
- 需要处理方块间的同步
- 世界交互复杂（点击、破坏）
- 外部管道连接混乱
- 性能开销大（7 个 BE vs 1 个）

**结论**：❌ 不推荐

---

### 方案 C：Block Attachments 方案（NeoForge 特有）

**核心思想**：使用 NeoForge 的 `BlockAttachments` API，将 6 个面的数据附着在同一个方块上

**优点：**
- 利用 NeoForge 新特性
- 数据分离更彻底

**缺点：**
- API 可能不够成熟
- 复杂度高，不如方案 A 直观
- 渲染和交互仍需自行处理

**结论**：⚠️ 可选，但推荐方案 A

---

## 12. 实现路线图

### 第一阶段：基础设施（P0）

1. 定义接口：`ZhenBusHost`、`SideProcessor`、`AbstractSideProcessor`
2. 实现数据存储：`ZhenBusStorage`、`SideProcessorData`
3. 实现容器：`ZhenBusContainer`

### 第二阶段：方块实体（P1）

4. 实现 `ZhenBusBlockEntity`（实现 `ZhenBusHost`）
5. 实现 `ZhenBusBlock`（方块注册、碰撞箱、交互）
6. 实现 `ZhenBusPartItem`（物品注册、安装/拆除）

### 第三阶段：核心逻辑提取（P2）

7. 从 `AbstractZhenBlockEntity` 提取 tick 逻辑到 `RecipeProcessor`
8. 创建 `SideProcessorMethod` 替代 `SmallSiftMethod`
9. 验证现有 ZhenType 可在新的 SideProcessor 上运行

### 第四阶段：面间通信（P3）

10. 实现面之间的跨面访问（如能量传输）
11. 实现 Capability 分发
12. 实现网络同步优化

### 第五阶段：渲染与交互（P4）

13. 实现合并模型渲染
14. 实现精确面点击命中检测
15. 实现 GUI 界面（如需要）

---

## 13. 性能优化附录（参考）

### 13.1 懒汉 Tick（推荐）

只对**有工作的面**执行 tick，避免空转：

```java
// ZhenBusBlockEntity
private final Set<Direction> activeFaces = new EnumSet.noneOf(Direction.class);

public void markFaceActive(Direction dir) { activeFaces.add(dir); }
public void markFaceIdle(Direction dir) { activeFaces.remove(dir); }

public static void serverTick(Level level, BlockPos pos, BlockState state, ZhenBusBlockEntity be) {
    for (Direction dir : be.activeFaces) {
        SideProcessor processor = be.container.get(dir);
        if (processor != null) processor.tick();
    }
}
```

**触发活跃/空闲的条件：**
| 活跃 | 空闲 |
|------|------|
| `inputsChanged = true` | 配方完成且无新配方匹配 |
| 有正在进行的配方 (`currentRecipe != null`) | 输入槽全部为空 |
| 有活跃的 `tickFactory` | `tickFactory` 返回空操作 |

### 13.2 Tick 间隔调度

对慢速配方（处理时间 > 100 tick）使用间隔 tick：

```java
// SideProcessor 基类
private int tickCounter = 0;
private int tickInterval = 1; // 默认每 tick 运行

public void setTickInterval(int interval) {
    this.tickInterval = Math.max(1, interval);
}

@Override
public void tick() {
    if (++tickCounter % tickInterval != 0) return;
    doActualTick();
}
```

| 配方处理时间 | 建议 tick 间隔 | 性能节省 |
|------------|---------------|---------|
| < 20 tick | 1（每 tick） | 0% |
| 20-100 tick | 2（半速） | 50% |
| 100-400 tick | 5（五分之一速） | 80% |
| > 400 tick | 10（十分之一速） | 90% |

### 13.3 空面板跳过

未安装任何处理器的面**不分配任何对象**：

```java
// ZhenBusContainer
private final Map<Direction, SideProcessor> processors = new EnumMap<>(Direction.class);
// 面为空时: 没有 Map Entry，没有 SideProcessor 对象
// 不进 tick 循环，不占内存

public boolean hasProcessor(Direction dir) {
    return processors.containsKey(dir);
}

public int activeCount() {
    return processors.size(); // 只有已安装的面才计入
}
```

### 13.4 配方匹配缓存

避免每 tick 都遍历配方列表：

```java
// SideProcessor
private @Nullable ZhenRecipe cachedRecipe;
private int recipeRecheckCounter = 0;
private static final int RECIPE_RECHECK_INTERVAL = 10; // 每 10 tick 检查一次

private void findRecipe() {
    if (recipeRecheckCounter++ % RECIPE_RECHECK_INTERVAL != 0) return;
    // 只有在 inputsChanged 时才真正执行 findRecipe
    if (!inputsChanged) return;

    ZhenRecipe newRecipe = ZhenRecipeManager.getInstance()
        .findRecipe(zhenType.getType(), items, partition, level);

    if (newRecipe != null && newRecipe.matchesFluid(tanks, partition)) {
        cachedRecipe = newRecipe;
        processTime = 0;
        inputsChanged = false;
    }
    // 若未找到，保留上次的 cachedRecipe（如果仍然匹配）
}
```

### 13.5 完整性能预算估算

```
单台 ZhenBus（6 个满配处理器）：

日常 tick（全部空闲，无配方）:
  遍历 6 个面 × 懒汉跳过 ✔ → 0ms

加工 tick（部分有配方进行中）:
  6 × (processTime++ + 布尔判断) ≈ 0.0012ms

配方完成 tick（每 100 tick 一次）:
  配方匹配 + 输入输出操作 ≈ 0.05ms

1000 台 ZhenBus（6000 处理器）:
  日常: 0ms（全懒汉跳过）
  加工中: 0.0012ms × 1000 = 1.2ms（远低于 50ms 上限）

结论：ZhenBus 架构本身不会成为性能瓶颈。
```

---

## 14. 风险和应对（原第13章）

| 风险 | 等级 | 应对 |
|------|------|------|
| NBT 体积增大（6 倍） | 🟡 | 分块懒加载，只加载活跃面 |
| Tick 性能开销 | 🟡 | 非活跃面降低 tick 频率，或使用 `LevelTicker` 调度 |
| 渲染性能 | 🟢 | 使用合并 BakedModel，单次 draw call |
| 与现有方块不兼容 | 🟡 | 保留现有 ZhenBlock，ZhenBus 作为新增 |
| 与其他模组管道交互 | 🟢 | Capability 按面分发，兼容性好 |
| 面冲突检测 | 🟢 | 每个面只能安装一个处理器，数据结构保证 |

---

## 15. 总结

本方案采用**方块实体内部多处理器**架构，在保留现有 MagicIO 全部系统（IOProcessor、SlotPartition、ZhenType、Recipe 等）的前提下，实现了一个方块 6 个面各自独立运行不同阵类型的目标。

**核心设计原则：**
- **最小改动**：尽量新增，少修改现有代码
- **逐步迁移**：现有 `ZhenBlock` 保留，`ZhenBus` 作为新增功能
- **模块化**：每个面是一个独立的 `SideProcessor`
- **性能优先**：1 个 BE 管理 6 个面，合并渲染

**与 AE2 Multipart 的核心差异：**
- AE2 有中心线缆（CablePart），ZhenBus 没有
- AE2 的 Part 通过 Grid 通信，ZhenBus 的 SideProcessor 通过共享 BlockEntity 通信
- AE2 支持覆盖板（Facade），ZhenBus 暂不需要

---

## 16. FlatZhen 单面阵设计（元面模式）

### 16.1 概述

除了 ZhenBus（一个方块 6 个面），还支持 **FlatZhen（单面阵，又名元面）**——每个 zhen 只对应一个面，贴在目标方块的某个面上，形状扁平的独立方块实体。

**与 ZhenBus 的关系：**

```
ZhenBus: 一个方块 → 6 个 SideProcessor（内部管理）
FlatZhen: 一个方块 → 1 个 SideProcessor（独立 BE）
```

FlatZhen 的本质是**将 ZhenBus 的某个面处理器独立出来作为一个单独的方块**，两者共用同一套 `SideProcessor` 和 `IOProcessor` 系统。

### 16.2 拓扑结构定义

FlatZhen 贴在某方块的一个面上后，其 IO 访问范围被定义为**方向组（DirectionGroup）**：

```
以元面贴在下方为例：

                  俯视图（上方看宿主方块）
                  北 W
             ┌──────────┐
             │  相邻方块  │
        ┌────┤          ├────┐
        │    │ 北面(邻)  │    │
        │    └────┬─────┘    │
   ┌────┴─────┐  │  ┌───┴──────┐
   │  相邻方块 │  │  │  相邻方块  │
   │          ├──┴──┤          │
   │ 西面(邻)  │ 宿主 │ 东面(邻)  │
   │          │ 方块 │          │
   └────┬─────┘      └────┬─────┘
        │    ┌────┬─────┘ │
        │    │ 南面(邻)    │
        └────┤          ├────┘
             └──────────┘
                  南

横截面：
         ┌──────────────────────┐
         │      宿主方块         │
         │  四面均可配置输入/输出  │
         └──────────┬───────────┘
                   │ ← 元面贴在这里
              ┌────┴────┐
              │  下方方块 │
              │  也参与连接 │
              └─────────┘
```

### 16.3 方向组映射

每个方向组覆盖**多个真实方块面**：

| 方向组名 | 覆盖的真实方块面 | 说明 |
|---------|----------------|------|
| **东面** | 宿主方块的东面 + 东侧相邻方块的东西南北四面与底面 + 下方方块的顶面与东面 | 向东侧延伸的所有可达面 |
| **西面** | 宿主方块的西面 + 西侧相邻方块的东面与底面 + 下方方块的顶面与西面 | 向西侧延伸的所有可达面 |
| **南面** | 宿主方块的南面 + 南侧相邻方块的北面与底面 + 下方方块的顶面与南面 | 向南侧延伸的所有可达面 |
| **北面** | 宿主方块的北面 + 北侧相邻方块的南面与底面 + 下方方块的顶面与北面 | 向北侧延伸的所有可达面 |
| **元面** | 元面直接接触宿主方块的那个面 | 与元面直接接触的核心方向 |

### 16.4 Zhen↔Zhen 互联规则

当两个 FlatZhen 的方向组存在交集时，它们之间自动建立直连通道：

```
示例：两个 zhen 的相互连接

        东面 Zhen（默认西方输出）
             │
             │ 方向组：东→西，交于元面的东输入
             ▼
         元面 Zhen（默认东方输入，西方输出）
             │
             │ 方向组：元面→西方向
             ▼
        西面 Zhen（默认东方输入）

逻辑：
  ① 东面 Zhen 产生输出 → 元面自动接收（东方输入）
  ② 元面处理完成 → 输出到西方
  ③ 西面 Zhen 自动从东方输入槽收到

  根本规则：当 A 的输出方向组与 B 的输入方向组重叠时，建立直连
```

### 16.5 虚拟端口（VirtualPort）

每个 FlatZhen 定义一组**虚拟端口**，每个端口对应一个方向组：

```java
public class VirtualPort {
    private final String name;                // "east_group", "west_group" ...
    private final PortRole role;              // INPUT, OUTPUT, BOTH
    private final List<PortBinding> bindings; // 映射到真实方块面

    // 运行时缓存（由邻居发现机制填充）
    private @Nullable SideProcessor connectedProcessor;
    private Direction connectedDirection;

    public boolean hasDirectConnection() {
        return connectedProcessor != null;
    }

    /** 尝试优先直传 zhen→zhen，失败则回退到世界 IO */
    public <T> T transfer(T value, IOType type, boolean simulate) {
        if (connectedProcessor != null) {
            IOComponent<?, ?> targetIO = connectedProcessor.getIOProcessor().get(type);
            if (targetIO != null) {
                T remain = targetIO.insert(value, simulate);
                // 如果全部接受，返回空；否则返回剩余的
                return remain;
            }
        }
        return value; // 回退到世界 IO（由调用方处理）
    }
}

public class PortBinding {
    private final Direction hostFace;   // 宿主方块的哪一个面
    private final Direction targetFace; // 目标方块的哪一个面
}
```

### 16.6 邻居发现与缓存机制

**核心原则：不在 tick 中扫描世界。** 仅在以下时机发现邻居：

```java
public class VirtualPort {

    /** 扫描所有绑定面，查找是否有 zhen 可直连 */
    public void scanNeighbors(Level level, BlockPos hostPos) {
        for (PortBinding binding : bindings) {
            BlockPos targetPos = hostPos.relative(binding.hostFace);
            BlockEntity be = level.getBlockEntity(targetPos);

            if (be instanceof FlatZhenBlockEntity flatZhen) {
                // 找到 FlatZhen，检查朝向是否匹配
                if (flatZhen.getAttachedFace() == binding.targetFace) {
                    this.connectedProcessor = flatZhen.getProcessor();
                    this.connectedDirection = binding.targetFace;
                    return; // 只缓存第一个找到的
                }

            } else if (be instanceof ZhenBusBlockEntity zhenBus) {
                // 找到 ZhenBus，检查对应面是否有处理器
                SideProcessor sp = zhenBus.getProcessor(binding.targetFace);
                if (sp != null) {
                    this.connectedProcessor = sp;
                    this.connectedDirection = binding.targetFace;
                    return;
                }
            }
            // 都不是 → 当作普通方块，走 Capability 世界 IO
        }
    }

    /** 当邻居方块被破坏或更新时调用，清除缓存 */
    public void invalidate() {
        this.connectedProcessor = null;
    }
}
```

**触发邻居扫描/失效的事件：**

| 事件 | 动作 |
|------|------|
| FlatZhen 放置时 | 扫描所有方向组，缓存邻居 |
| 邻居方块放置/更新 | 重新扫描受影响的方向组 |
| 邻居方块被破坏 | 通过 `onNeighborChanged` 失效缓存 |
| 邻居的 zhen 被拆除 | 通过 PartChanged 事件失效缓存 |
| 每 200 tick（安全兜底） | 所有缓存超时，重新扫描 |

在 tick 中运行时，`VirtualPort.transfer()` 只是**检查已缓存的引用是否为 null**，不是扫描世界，所以每次直传的成本仅为一次 null 检查 + 一次方法调用。

### 16.7 优先级路由逻辑

每个 FlatZhen 的输出遵循以下优先级：

```
输出逻辑（按优先级从高到低）：

① 有通过方向组直连的 zhen 吗？
   ├── 有 → 尝试 insert 到对面 zhen 的 IOProcessor
   │       ├── 对面能接受全部 → 完成，返回
   │       └── 对面只能接受部分 → 剩余部分走②
   └── 无 → 直接走②

② 写入自己的输出槽（SlotZone.OUTPUT）
   ├── 能写入 → 完成
   └── 不能 → 物品留在原地，等待下个 tick
```

### 16.8 方案决策：为什么不合并 I/O

你问过："当 A 的输出方向组与 B 的输入方向组重叠时，是否可以合并为一个共用 I/O 池？"

**答案：不推荐合并。** 原因如下：

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| 物品所有权 | 🔴 | 共用池的物品归谁？A 认为已输出，B 认为未输入，配方取消时退给谁？|
| NTT 序列化 | 🔴 | 存档时共用池归谁序列化？A 还是 B？ |
| SlotPartition 冲突 | 🟡 | 共用池跨越两个 zhen 的 Zone 定义，SlotPartition 无法表达 |
| 面增减困难 | 🔴 | 拆除一个 zhen 时共用池如何分裂？拆分后多余的物品如何处理？|
| 配方匹配二义性 | 🟡 | B 匹配配方时是否把 A 输出槽中的物品也算作自己的输入？ |

**优先路由（16.7）方案是功能完整性和实现复杂度之间的最佳平衡。**

### 16.9 备选方案：事件总线

当 6 个以内的节点互联时，优先路由 + 缓存引用足够。如果未来节点数量膨胀到**成百上千**且需要复杂互联，可以考虑引入事件总线：

```
                   Event Bus
               ┌───────────────┐
               │  ItemAvailable │
               │  FluidAvail   │
               │  EnergyAvail  │
               └───────┬───────┘
         ┌──────────────┼──────────────┐
         │              │              │
    West Zhen       元面 Zhen      East Zhen
    (发布者)        (消费者)       (消费者)
```

但对于当前设计，事件总线属于**过度设计**，不推荐。

---

## 17. 大规模性能压力测试分析

### 17.1 测试场景设定

```
方块数：   200 个（每个方块可承载多个 zhen）
处理器数： 600 个（平均 3 个/方块）
互联：     全部通过方向组直连
工作状态： 全速合成（每个 zhen 都在进行配方）
配方：     每条配方 ≤2 个输入，≤2 个输出，processingTime 20-100 tick
配方总数： 200 个（跨越多个类型）
```

### 17.2 逐层定量分析

**17.2.1 tick 循环本身（完全不用担心）**

```java
blockEntity.processTime += 1;            // 1 次整数加法
if (blockEntity.processTime >= ...) {}   // 1 次比较
```
600 个 zhen 做这件事 ≈ **0.001ms/tick**。可以忽略。

**17.2.2 配方匹配（最大变量）**

`ZhenRecipeManager.findRecipe()` 的实现：

```java
public ZhenRecipe findRecipe(String type, NonNullList<ItemStack> allItems,
        SlotPartition partition, Level level) {
    for (ZhenRecipe recipe : recipes) {                    // ← 遍历全部配方
        if (recipe.getTypeStr().equals(type)                // ← 字符串比较
                && recipe.matches(allItems, partition, level)) { // ← 三层循环
            return recipe;
        }
    }
    return null;
}
```

`recipe.matches()` 内部：

```java
for (RecipeInput<?> input : inputs) {             // 遍历输入条件（~2次）
    for (Ingredient ingredient : ingredients) {    // 遍历每种材料（~2次）
        for (int slot : partition.getSlots(...)) { // 遍历槽位（~3次）
            ingredient.test(allItems.get(slot))   // 逐个测试
        }
    }
}
```

```
200 个配方 × 2 个输入 × 2 种材料 × 3 个槽位
= 2400 次 ingredient.test() 调用
≈ 0.02ms / 次 findRecipe
```

**17.2.3 跨 zhen 传递（缓存引用直传）**

```java
// VirtualPort.transfer() - 不是每 tick 扫描世界，只是检查缓存引用
if (connectedProcessor != null) {               // null 检查
    IOComponent<?, ?> targetIO = processor.get(type); // HashMap.get()
    targetIO.insert(value, simulate);                 // 插入
}
```
```
每次传递 = 1 次 null 检查 + 1 次 HashMap.get() + 1 次 List 操作
≈ 0.002ms / 次传递

600 个 zhen 同时传递 → 600 × 0.002ms = 1.2ms（仍然安全）
```

**17.2.4 配方完成产出（GC 压力）**

配方完成时：创建新的 `ItemStack`、`NonNullList`、触发随机数生成（`rollOutput`）。
这是**分配压力 > CPU 压力**：

```
每次配方完成：创建 1-4 个 ItemStack + 1 个 NonNullList
正常频率（20 tick/次）：每秒 1-3 次 → 无感
极端频率（600 同时完成）：同时分配 1200-2400 个对象
→ JVM Young GC pause ~5-10ms，可能被感知为瞬间卡顿
```

**17.2.5 网络同步**

```
每次完成调一次 sendBlockUpdated()
每个数据包 ~100 字节
正常频率：几 tick 一次 → 忽略不计
```

### 17.3 三种场景定量对比

#### 场景 A：日常运行（~50% 活跃）

| 操作 | 单位成本 | 频次/tick | 总计 |
|------|---------|-----------|------|
| 活跃 tick（processTime++） | 0.0002ms | 300 次 | 0.06ms |
| 配方匹配（findRecipe） | 0.02ms | 3 次（输入变化时） | 0.06ms |
| 配方完成（roll + consume + produce） | 0.05ms | 3 次 | 0.15ms |
| 跨 zhen 传递 | 0.002ms | 3 次 | 0.006ms |
| **总计** | | | **≈ 0.3ms** |

> 仅占 50ms tick 预算的 **0.6%**。完全无感。

#### 场景 B：全速冲刺（600 全部活跃）

| 操作 | 单位成本 | 频次/tick | 总计 |
|------|---------|-----------|------|
| 活跃 tick | 0.0002ms | 600 次 | 0.12ms |
| 配方匹配 | 0.02ms | 6 次（完成后重匹配） | 0.12ms |
| 配方完成 + 产出 | 0.05ms | 6 次 | 0.30ms |
| 跨 zhen 传递 | 0.002ms | 6 次 | 0.012ms |
| 网络同步 | 0.01ms | 6 次 | 0.06ms |
| **总计** | | | **≈ 0.6ms** |

> 占 50ms 预算的 **1.2%**。仍远低于卡顿阈值。

#### 场景 C：同步爆发（所有 600 个同步完成）

| 操作 | 单位成本 | 频次 | 总计 |
|------|---------|------|------|
| 配方匹配 | 0.02ms | 600 次 | **12ms** |
| rollOutput（GC 压力） | 0.03ms | 600 次 | **18ms** |
| 物品产出 + 跨 zhen 传递 | 0.01ms | 600 次 | 6ms |
| 网络同步 | 0.01ms | 200 个方块 | 2ms |
| **总计** | | | **≈ 38ms（76% 预算）** |

> 导致 TPS 从 20 掉到 **~16**，玩家能感觉到卡顿。

**但是，此场景在现实中几乎不可能发生：**
1. 所有 zhen 的 `processingTime` 必须完全相同
2. 所有 zhen 必须在同一 tick 启动
3. 输入槽必须同时被填满
4. 玩家实际操作中，启动时间是错开的

### 17.4 真正瓶颈：findRecipe 线性扫描

**结论：** 配方匹配是大规模场景下**唯一真正的瓶颈**。其他操作（整数加减、布尔判断、List 操作）的成本对现代 JVM 来说微不足道。

你的 `ZhenRecipeManager.findRecipe()` 使用**全局线性扫描**：

```java
// 当前实现
for (ZhenRecipe recipe : recipes) { // 遍历所有配方（~200 个）
    if (recipe.getTypeStr().equals(type) && recipe.matches(...)) { ... }
}
```

如果配方数量增长到 1000+，这个函数的耗时将线性增长。

### 17.5 findRecipe 优化方案

**优化方案：按 type 分组（推荐）**

```java
public class ZhenRecipeManager {
    private final Map<String, List<ZhenRecipe>> recipesByType = new HashMap<>();

    public void addRecipe(ZhenRecipe recipe) {
        recipesByType.computeIfAbsent(
            recipe.getTypeStr(), k -> new ArrayList<>()
        ).add(recipe);
    }

    public ZhenRecipe findRecipe(String type, NonNullList<ItemStack> allItems,
            SlotPartition partition, Level level) {
        List<ZhenRecipe> candidates = recipesByType.get(type);
        if (candidates == null) return null;
        for (ZhenRecipe recipe : candidates) { // 只遍历同类型的配方
            if (recipe.matches(allItems, partition, level)) {
                return recipe;
            }
        }
        return null;
    }
}
```

**优化效果：**

| 配方总数 | 同类型配方数 | 优化前 | 优化后 | 提升倍数 |
|---------|------------|--------|--------|---------|
| 200 | 20 | 0.02ms | 0.002ms | **10x** |
| 1000 | 50 | 0.10ms | 0.005ms | **20x** |
| 5000 | 100 | 0.50ms | 0.01ms | **50x** |

**改动量：** 约 10 行代码，不改接口、不改调用方。

**其他可选优化（如需）：**

| 优化 | 效果 | 复杂度 |
|------|------|--------|
| 配方缓存（inputsChanged 时才查找） | 减少 99% 的查询 | 低 |
| 按元素类型二次分组 | 进一步缩小候选集 | 低 |
| 输入 Ingredient 哈希索引 | O(1) 匹配 | 高（收益有限） |

---

## 18. 推荐实施路径（优先级清单）

### P0: 基础设施构建（必须先完成）

| 步 | 内容 | 目标文件 | 说明 |
|---|------|---------|------|
| 0.1 | 定义 `SideProcessor` 接口 | `SideProcessor.java` | tick()、getIOProcessor()、getShape()、NBT 读写、onActivate、生命周期 |
| 0.2 | 实现 `AbstractSideProcessor` 基类 | `AbstractSideProcessor.java` | 持有 Direction、ZhenType、IOProcessor、独立 items/tanks、懒汉 tick 支持 |
| 0.3 | 提取 `RecipeProcessor` 工具类 | `RecipeProcessor.java` | 将 AbstractZhenBlockEntity.serverTick 中的配方处理逻辑提取为无状态静态方法 |
| 0.4 | 优化 `ZhenRecipeManager` | `ZhenRecipeManager.java` | 改为 Map<String, List> 按 type 分组，避免线性扫描 |

### P1: ZhenBus 方块（最核心）

| 步 | 内容 | 目标文件 | 说明 |
|---|------|---------|------|
| 1.1 | 实现 `ZhenBusStorage` | `ZhenBusStorage.java` | 纯数据结构：Map<Direction, SideProcessor> + 空面跳过 |
| 1.2 | 实现 `ZhenBusContainer` | `ZhenBusContainer.java` | addProcessor、removeProcessor、tickAll、合并碰撞箱、序列化 |
| 1.3 | 定义 `ZhenBusHost` 接口 | `ZhenBusHost.java` | getProcessor、canAddProcessor、markForUpdate、markForSave |
| 1.4 | 实现 `ZhenBusBlockEntity` | `ZhenBusBlockEntity.java` | 实现 ZhenBusHost，serverTick 调用 container.tickAll，Capability 按面分发 |
| 1.5 | 实现 `ZhenBusBlock` | `ZhenBusBlock.java` | 继承 BaseEntityBlock，碰撞箱 → container.getCombinedShape，交互 → 按面转发 |

### P2: FlatZhen 单面阵

| 步 | 内容 | 目标文件 | 说明 |
|---|------|---------|------|
| 2.1 | 定义 `VirtualPort` 和 `PortBinding` | `VirtualPort.java` | 方向组映射、邻居缓存引用、transfer 优先路由 |
| 2.2 | 实现 `FlatZhenBlockEntity` | `FlatZhenBlockEntity.java` | 单面版 BE，持有 1 个 SideProcessor + 6 个 VirtualPort（东南西北元面） |
| 2.3 | 实现 `FlatZhenBlock` | `FlatZhenBlock.java` | 类按钮/压力板的薄方块(1/16)，FACING 属性，碰撞箱极小 |
| 2.4 | 实现虚拟端口邻居发现 | 在 FlatZhenBlockEntity 中 | 放置/邻居更新时 scanNeighbors，破坏时 invalidate，每 200 tick 兜底刷新 |

### P3: 物品安装与交互

| 步 | 内容 | 目标文件 | 说明 |
|---|------|---------|------|
| 3.1 | 实现 `ZhenBusPartItem` | `ZhenBusPartItem.java` | 携带 ZhenType 数据的物品，右击面安装到 ZhenBus/附着到方块 |
| 3.2 | 安装验证 | （同上） | 面是否为空、兼容性检查、消耗物品、创建 SideProcessor |
| 3.3 | 拆除逻辑 | （同上） | 潜行/扳手拆除，掉落物品和处理器内容 |
| 3.4 | GUI 支持（如需） | `SideProcessorScreen.java` | 每个面可独立打开 GUI（使用内置菜单） |

### P4: 面间交互与渲染

| 步 | 内容 | 目标文件 | 说明 |
|---|------|---------|------|
| 4.1 | 实现 Capability 按面分发 | ZhenBusBlockEntity | getCapability 时根据 side 路由到对应 SideProcessor 的 IOComponent |
| 4.2 | ZhenBus 面间能量传输（如需） | ZhenBusContainer | 跨面查询：getNeighbor(fromSide, direction) |
| 4.3 | 网络同步优化 | 相关文件 | 块状同步：只发送有变化的面，使用 bitmask 标记 |
| 4.4 | 合并渲染 | `ZhenBusBakedModel.java` | 每个面独立模型 → 合并为单个 BakedModel |
| 4.5 | 碰撞箱精确命中 | 相关文件 | 点击位置 → selectProcessor → 转发交互到对应面 |

### P5: 性能与安全

| 步 | 内容 | 目标文件 | 说明 |
|---|------|---------|------|
| 5.1 | 懒汉 tick | `AbstractSideProcessor` | 无工作面的面不进 tick（30 行判断逻辑） |
| 5.2 | Tick 间隔调度 | `AbstractSideProcessor` | 慢速配方减少 tick 频率（20 行计数器） |
| 5.3 | 配方匹配缓存 | `AbstractSideProcessor` | inputsChanged 标志 + 间隔检查（10 行） |
| 5.4 | 空面跳过 | `ZhenBusContainer` | 未安装处理器的面不分配内存（无需修改，EnumMap 天然支持） |

### 优先级总览

```
P0 ████████████████████  基础设施（4 步）
P1 ████████████████████  ZhenBus（5 步）       ← 核心功能
P2 ██████████            FlatZhen（4 步）      ← 扩展功能
P3 ████████              安装/交互（4 步）
P4 ██████                面间交互/渲染（5 步）
P5 ████                  性能优化（4 步）

建议先完成 P0，获得 SideProcessor + RecipeProcessor + 优化的 RecipeManager。
然后 P1 完成 ZhenBus，获得 6 面并行能力。
再走 P2 获得 FlatZhen 单面阵。
P3-P5 按需推进。
```

### 各阶段交付物

| 阶段 | 完成标志 | 可测试内容 |
|------|---------|-----------|
| P0 | 编译通过，ZhenRecipeManager 单元测试通过 | 旧方块不受影响 |
| P1 | 游戏中可放置 ZhenBus，各面独立运行不同配方 | 6 个面同时运行 |
| P2 | 可手持 PartItem 将 FlatZhen 贴在方块上 | 单面独立运行 |
| P3 | 可用扳手拆除面的处理器，掉落正确 | 安装/拆除循环 |
| P4 | 外部管道从不同面接入能正确路由 | 与其他模组管道配合 |
| P5 | 1000 方块压力测试，TPS 不低于 19.5 | 大规模场景稳定 |
